name: CI
on: [push]
jobs:
  zig-test:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: zig test src/tests.zig 
      - run: zig build
